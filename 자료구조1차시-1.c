#include <stdio.h>
#include <stdlib.h>
#include <time.h>



void main() {
    int num[50];
    int n;
    int i;
    srand(time(NULL));
    scanf("%d", n);

    if(i=0; i<n; i++) {
        num[i]=rand()*101;
        printf("%d ", A[i]);
    }
}